#ifndef ACTOR_H
#define ACTOR_H

#include <QObject>

/*class Actor: public QObject
{
    Q_OBJECT

    Q_PROPERTY(int id
               READ id
               CONSTANT)
    Q_PROPERTY(QString movie_id
               READ movie_id
               CONSTANT)
    Q_PROPERTY(QString imdb_id
               READ imdb_id
               CONSTANT)
    Q_PROPERTY(QString name
               READ imdb_id
               CONSTANT)
public:
    Actor();
};*/

#endif // ACTOR_H
